/* prova caricamento schede con lo scorrere della pagina, 
non sono riuscito a implementarlo in modo che sia fruibile da ogni dispositivo */

/*   let watchItAgain = document.getElementsByClassName("watch_it_again");
  let newReleases = document.getElementsByClassName("new_releases");
  

window.addEventListener('scroll', function page() {
  if (window.innerWidth < 576) {
    watchItAgain[0].className += " show1";
  };
  if ((window.innerWidth > 575) && (window.innerWidth < 768)) {
    if ( window.pageYOffset > 290) {
      watchItAgain[0].className += " show1";
    } 
    if ( window.pageYOffset > 730) {
      newReleases[0].className += " show1";
    }
  };
  if ((window.innerWidth > 767) && (window.innerWidth < 992)) {
    if ( window.pageYOffset > 100) {
      watchItAgain[0].className += " show1";
    } 
    if ( window.pageYOffset > 500) {
      newReleases[0].className += " show1";
    }
  };
  if ((window.innerWidth > 991) && (window.innerWidth < 1200)) {
    if ( window.pageYOffset > 100) {
      watchItAgain[0].className += " show1";
    } 
    if ( window.pageYOffset > 570) {
      newReleases[0].className += " show1";
    }
  };
  
}); */

